<%= form_with(model: hamburguesa, local: true) do |form| %>
  <% if hamburguesa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hamburguesa.errors.count, "error") %> prohibited this hamburguesa from being saved:</h2>

      <ul>
        <% hamburguesa.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion %>
  </div>

  <div class="field">
    <%= form.label :imagen %>
    <%= form.text_field :imagen %>
  </div>

  <div class="field">
    <%= form.label :precio %>
    <%= form.text_field :precio %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
